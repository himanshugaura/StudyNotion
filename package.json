{
  "name": "project-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n \"frontend,server\" -c \"bgBlue,bgYellow\" \"npm run dev --prefix frontend\" \"npm run dev --prefix server\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "@ramonak/react-progress-bar": "^5.3.0",
    "dotenv": "^16.4.7",
    "react-hook-form": "^7.54.2",
    "react-otp-input": "^3.1.1"
  }
}
